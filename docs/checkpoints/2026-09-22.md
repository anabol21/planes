# Checkpoint — 2026-09-22

## Purpose

Review runnable evidence from each stream and decide the smallest end-to-end slice that can be integrated without hiding contract gaps.

## Required demos

### Grisha

- Run the same simplified scenario under `min_time` and `min_flight_hours`.
- Show feasibility checks and objective values.
- Compare at least one small instance with an exact baseline.
- State every approximation and unsupported feature.

### Ruslan

- Submit a job and show `queued → running → completed` through the API.
- Repeat with fake infeasible, crash, and timeout outcomes.
- Show persistence and atomic worker claim.

### Misha

- Run a versioned request through the compute boundary locally and on VPS.
- Show job-correlated logs and distinct success/crash/timeout mapping.
- Reproduce setup from the runbook without committed secrets.

## Joint decisions

- Freeze `ComputeRequest/ComputeResponse v0` fixtures.
- Decide whether the next integration uses in-process, subprocess, container, or network transport.
- Assign the Q-CHECK owner and define the first independent oracle.
- Record mismatches between model output and backend/runtime expectations.
- Replan only after evidence is shown.

## Exit gate

- [ ] Three status files updated with commits and commands.
- [ ] Each PR reviewed by someone other than its author.
- [ ] Shared fixture accepted.
- [ ] One vertical-slice smoke test defined.
- [ ] Risks and deferred scope recorded without claiming unsupported capability.
