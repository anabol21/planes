# Checkpoint — 2026-09-23

## Purpose

Record what Misha confirmed on the evening of 2026-09-23, and the subset of that work landed on `main`. Contract version remains `v0`.

## Confirmed

### Ruslan — backend architecture

- Branch `backend/RUS-001-pipeline`, tip `0d61960` (2026-09-22 21:05 +0300), message `feat(backend): implement RUS-001 async job pipeline`.
- Stdlib WSGI API (`POST /jobs`, `GET /jobs/{id}`, `GET /jobs/{id}/result`), SQLite job store, separate worker, backend-local `OptimizationEngine` port, and deterministic `FakeOptimizationEngine`.
- The worker calls `engine.solve`. It does not import solver internals.
- Covers the RUS-001 slice `REQ-PROD-001`, `REQ-PROD-002`, `REQ-PROD-003`, `REQ-OPT-003`, and `REQ-DOC-004`. It does not prove route quality.
- Still depends on `OPEN-001` through `OPEN-006`, `OPEN-019`, and `OPEN-021`. Backend types stay local. `src/planes/contracts/**` was not rewritten.

### Grisha — two-method baseline

- `model/GRI-001-simplified-optimizer` matches `dev` (`4c459a6`) and contains no solver.
- The solver is on `GreforyAbdr-patch-1`: `db35d22` (2026-09-23 20:15 +0300, `Create first`) and `90d1200` (2026-09-23 20:30 +0300, `Basic_model_attemp_first`).
- Two methods under `src/planes/model/basic_model/gibrid-optimizer/`: MILP (`optimizer/milp_solver.py`) and metaheuristic (`optimizer/metaheuristic.py`). Selector objectives are `min_time` and `min_flight_hours`.
- This is inside `src/planes/model/**`. It is not the body of `src/planes/runtime/solver.py`. That function still raises `NotImplementedError`.
- Covers the GRI-001 slice `REQ-PROD-002`, `REQ-PLAN-001` through `REQ-PLAN-004`, `REQ-OPT-001`, and `REQ-OPT-002`.
- Still depends on `OPEN-002`, `OPEN-003`, `OPEN-006` through `OPEN-012`, and `OPEN-014` through `OPEN-016`. The metaheuristic status is `heuristic`.

### Misha — runtime contour

- Already on `main` at `5c2cc9e` (VPS contour and runbook). This landing does not change `src/planes/runtime/**`, does not redeploy the VPS, and does not commit a compute token or VM address.

## Landed

- `02011ce` merges `backend/RUS-001-pipeline` onto `5c2cc9e`. No conflicts.
- `105b712` merges the optimizer sources from `GreforyAbdr-patch-1`. No conflicts with `main`.

## Left out

From `GreforyAbdr-patch-1`, before the merge commit:

- `src/planes/model/basic_model/first` — a single newline from `Create first`.
- `src/planes/model/basic_model/geoscan-optimizer` — gitlink `9b279b14` with no `.gitmodules` and no object in this clone.
- `gibrid-optimizer/.idea/**` — local IDE files.
- `gibrid-optimizer/data/outputs/**` — generated solver JSON.

Kept `requirements.txt.txt` as committed. The README installs `requirements.txt`.

`demo/end-to-end-mvp` (`3c1c60c`, 2026-09-23 21:55 +0300) is inside the checkpoint window and was not merged. It adds the WEB-001 client, KML demo workflow, Windows quick start, and a worker `--engine runtime` composition (`runtime_engine.py`). That is outside the confirmed architecture and the two-method baseline. The RUS-001 pipeline already builds and tests with the fake engine. No later commit was required to make the confirmed work build or test.

No other remote branch holds this work. Fetched remotes: `origin` only.

## Commits after 2026-09-23 Europe/Moscow

None. Latest remote commit is `3c1c60c` at 2026-09-23 21:55 +0300, and it stays on `demo/end-to-end-mvp`.

## Exit evidence

- `PYTHONPATH=src python3 -m unittest discover -s tests/backend -v` — `Ran 20 tests in 0.876s` / `OK`.
- `PYTHONPATH=src python3 -m unittest discover -s tests/runtime -v` — `Ran 28 tests in 7.244s` / `OK`. Default `solver.solve` path still reports `NotImplementedError`.
- From `src/planes/model/basic_model/gibrid-optimizer`: `python3 -m optimizer.main` for `min_flight_hours` and `min_time`, each with `--solver milp` and `--solver meta`. MILP returned `optimal`; meta returned `heuristic`. The two criteria used 1 and 2 UAVs respectively on this fixture (N=2, M=10). Eight validation flags were true on each run. `unittest discover` on `gibrid-optimizer/tests` ran 0 tests.
- `python3 scripts/validate_workspace.py` — `Workspace validation: PASS`.
