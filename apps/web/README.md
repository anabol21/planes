# Web client — reserved

Frontend implementation starts after the API payload and first vertical slice are stable. It will consume backend endpoints only and will not calculate routes or optimization results in the browser.
