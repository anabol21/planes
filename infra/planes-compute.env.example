# Copy to /etc/planes/planes-compute.env (mode 600, outside git).
# Replace every value before starting the listener. Do not commit the copy.
COMPUTE_HOST=replace-with-compute-host
COMPUTE_TOKEN=replace-with-a-long-random-token
COMPUTE_TIMEOUT_SECONDS=120
