# Infrastructure scope

Deployment manifests, VPS runbooks, health checks, and runtime configuration templates live here. Commit templates only; real secrets and host credentials stay outside Git.

Compute listener templates: `planes-compute.service`, `planes-compute.env.example`, `bootstrap-vps.sh`. Operations notes are in `runbook.md`.
